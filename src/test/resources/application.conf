test {
  kafka {
    url = "localhost:6001"
    topic {
      incoming = ["foo"]
      outgoing {
        default = "bar"
      }
    }
  }
}

test-with-error {
  kafka {
    url = "localhost:6001"
    topic {
      error = "error"
      incoming = ["foo"]
      outgoing {
        default = "bar"
      }
    }
  }
}

akka {
  loggers = ["akka.event.slf4j.Slf4jLogger"]
  loglevel = "DEBUG"
  logging-filter = "akka.event.slf4j.Slf4jLoggingFilter"
}